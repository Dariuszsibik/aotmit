export const login = (email, password) => {
  // w realnej sytuacji mielibyśmy tutaj jakieś zapytanie do api
  // fetch('http://my-login-url.com', {body: JSON.stringify({email,password},),method:'POST'})
  // console.log("Login with:",{email,password})

  return Promise.resolve();
  // return Promise.reject('Niepoprawne dane logowania');
};
